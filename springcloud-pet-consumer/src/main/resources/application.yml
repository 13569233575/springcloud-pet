
server:
  port: 9001


eureka:
  client:
    service-url:
      defaultZone: http://admin:zhzy@10.10.11.172:7001/eureka
  instance:
    prefer-ip-address: true # �ڵ�ַ����ʹ��IP��ַ������ʾ
    instance-id: @artifactId@

feign:
  hystrix:
    enabled: true
  compression:
    request:
      enabled: true
      mime-types: # ���Ա�ѹ��������
        - text/xml
        - application/xml
        - application/json
      min-request-size: 2048 # ����2048���ֽڽ���ѹ��

logging:
  level:
    com.zhzy: DEBUG

spring:
  application:
    name: springcloud-pet-consumer

info:
  app.name: springcloud-pet-consumer
  company.name: com.zhzy
  build.artifactId: $project.artifactId$
  build.modelVersion: $project.modelVersion$